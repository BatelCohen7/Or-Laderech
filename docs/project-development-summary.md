# רשימת פיתוח פרויקט "אור בדרך"

## מודולים שפותחו ✅

### דפים ראשיים
1. **דשבורד דיירים** (`src/pages/ResidentDashboard.tsx`)
   - דף ראשי לדיירים עם גישה לכל המודולים
   - כולל תצוגת סקירה כללית, ציר זמן, הצבעות ועוד

2. **דשבורד מנהל** (`src/pages/AdminDashboard.tsx`)
   - דף ראשי למנהלי המערכת
   - כולל ניהול פרויקטים, דיירים, מסמכים ועוד

3. **דף בית** (`src/pages/HomePage.tsx`)
   - דף הבית של האתר
   - כולל תצוגת פרויקטים ושירותים

4. **דף אודות** (`src/pages/AboutPage.tsx`)
   - דף אודות החברה
   - כולל מידע על החברה והצוות

5. **דף שירותים** (`src/pages/ServicesPage.tsx`)
   - דף שירותי החברה
   - כולל פירוט השירותים והמחירים

6. **דף צור קשר** (`src/pages/ContactPage.tsx`)
   - דף יצירת קשר
   - כולל טופס יצירת קשר ופרטי התקשרות

7. **תצוגת פרויקטים** (`src/pages/ProjectsPage.tsx`)
   - תצוגת פרויקטים פעילים
   - כולל סינון וחיפוש

8. **תצוגת פרויקט בודד** (`src/pages/TzfatProjectPage.tsx`)
   - תצוגת פרטי פרויקט בודד
   - כולל גלריה, ציר זמן ופרטי קשר

9. **ניתוח זכויות תכנוניות** (`src/pages/PlanningRightsPage.tsx`)
   - ניתוח זכויות בנייה ותכנון
   - כולל חיפוש לפי כתובת או גוש/חלקה

10. **תצוגת מודל תלת-ממדי** (`src/pages/PropertyModel3DPage.tsx`)
    - תצוגת מודל תלת-ממדי של נכס
    - אינטגרציה עם Google Maps

11. **הרשמת דיירים** (`src/pages/ResidentRegistrationPage.tsx`)
    - טופס הרשמה לדיירים
    - כולל אימות נתונים

12. **כניסת דיירים** (`src/pages/ResidentLogin.tsx`)
    - טופס כניסה לדיירים
    - תומך בכניסה עם ת.ז או אימייל

13. **כניסת משתמשים** (`src/pages/LoginPage.tsx`)
    - טופס כניסה למשתמשים
    - תומך בסוגי משתמשים שונים

14. **דשבורד כללי** (`src/pages/DashboardPage.tsx`)
    - דשבורד כללי למשתמשים
    - מותאם לסוג המשתמש

### רכיבי דיירים
1. **מערכת הצבעות** (`src/components/ResidentManagement/VotingSystem.tsx`)
   - מערכת להצבעות דיגיטליות
   - תומך בסוגי הצבעות שונים ותצוגת תוצאות

2. **מרכז תקשורת** (`src/components/ResidentManagement/CommunicationCenter.tsx`)
   - מערכת להודעות ותקשורת בין הגורמים
   - כולל הודעות פרטיות וקבוצתיות

3. **ציר זמן פרויקט** (`src/components/ResidentManagement/ProjectTimeline.tsx`)
   - תצוגת ציר זמן של הפרויקט
   - מציג אבני דרך והתקדמות

4. **מרכז תמיכה** (`src/components/ResidentManagement/EnhancedSupportCenter.tsx`)
   - מערכת תמיכה ומענה לשאלות
   - כולל שאלות נפוצות וטפסי פנייה

5. **מרכז התנגדויות** (`src/components/ResidentManagement/ObjectionsCenter.tsx`)
   - מערכת להגשת התנגדויות
   - כולל מעקב אחר סטטוס ההתנגדויות

6. **מרכז ידע** (`src/components/ResidentManagement/KnowledgeCenter.tsx`)
   - מאגר מידע ומדריכים
   - כולל מאמרים, מדריכים וסרטוני הדרכה

7. **ניהול פרופיל** (`src/components/ResidentManagement/ResidentProfile.tsx`)
   - עריכת פרטי משתמש
   - כולל עדכון פרטים אישיים ופרטי דירה

8. **מודלים תלת-ממדיים** (`src/components/ResidentManagement/PropertyManagement3D.tsx`)
   - תצוגת מודלים תלת-ממדיים
   - אינטגרציה עם Google Maps

### רכיבי מנהל
1. **ניהול פרויקטים** (`src/components/AdminProjectCard.tsx`)
   - ניהול פרויקטי התחדשות עירונית
   - כולל יצירה, עריכה ומחיקה של פרויקטים

2. **ניהול דיירים** (`src/components/AdminResidentTable.tsx`)
   - ניהול דיירים בפרויקטים
   - כולל ייבוא/ייצוא נתונים

3. **ניהול מסמכים** (`src/components/AdminDocumentList.tsx`)
   - ניהול מסמכים בפרויקטים
   - כולל אישור/דחיית מסמכים

4. **ניהול הצבעות** (`src/components/AdminVotingSystem.tsx`)
   - ניהול הצבעות בפרויקטים
   - כולל יצירת הצבעות חדשות וסגירת הצבעות

5. **ניהול התנגדויות** (`src/components/AdminObjectionsList.tsx`)
   - ניהול התנגדויות בפרויקטים
   - כולל אישור/דחיית התנגדויות

6. **ניהול מרכז ידע** (`src/components/AdminKnowledgeCenter.tsx`)
   - ניהול תוכן במרכז הידע
   - כולל יצירה, עריכה ומחיקה של תוכן

### רכיבי UI כלליים
1. **רכיב נגישות** (`src/components/AccessibilityWidget.tsx`)
   - רכיב להגדרות נגישות
   - כולל שינוי גודל טקסט, ניגודיות ועוד

2. **רכיב חיפוש גלובלי** (`src/components/GlobalSearch.tsx`)
   - חיפוש בכל תוכן האתר
   - תומך בסוגי תוכן שונים

3. **רכיב החלפת שפה** (`src/components/LanguageSwitcher.tsx`)
   - החלפת שפת הממשק
   - תומך בעברית, אנגלית ורוסית

4. **כרטיס פרויקט צפת** (`src/components/TzfatCanaanProjectCard.tsx`)
   - תצוגת פרויקט צפת - שכונת כנען
   - כולל פרטי פרויקט ותצוגה ויזואלית

5. **רכיבי UI בסיסיים** (`src/components/ui/`)
   - כפתורים, כרטיסים, תיבות קלט, תגיות ועוד
   - עיצוב אחיד ומותאם לפרויקט

### שירותים ותשתית
1. **שירות אימות** (`src/contexts/AuthContext.tsx`)
   - ניהול התחברות והרשאות משתמשים
   - תומך בסוגי משתמשים שונים

2. **שירות שפה** (`src/contexts/LanguageContext.tsx`)
   - ניהול שפת הממשק
   - תומך בעברית, אנגלית ורוסית

3. **שירות מסד נתונים** (`src/services/databaseService.ts`)
   - ניהול הקשר עם מסד הנתונים
   - תומך בפעולות CRUD

4. **שירות API** (`src/services/api.ts`)
   - ניהול קריאות API
   - תומך בפעולות שונות מול השרת

5. **שירות אימות נתונים** (`src/utils/validation.ts`)
   - אימות נתונים בטפסים
   - כולל אימות ת.ז ישראלית

## מודולים שטרם פותחו 🔜

### אבטחה מתקדמת
1. **אימות דו-שלבי** (`src/components/TwoFactorAuth.tsx`)
   - אימות דו-שלבי לכניסה למערכת
   - יתמוך ב-SMS, אפליקציה ואימייל

2. **אימות ביומטרי** (`src/components/BiometricAuth.tsx`)
   - אימות ביומטרי לכניסה למערכת
   - יתמוך בטביעת אצבע וזיהוי פנים

3. **התחברות עם רשתות חברתיות** (`src/components/SocialLogin.tsx`)
   - התחברות באמצעות רשתות חברתיות
   - יתמוך ב-Google, Facebook, Apple

### תקשורת מתקדמת
1. **צ'אט בזמן אמת** (`src/components/LiveChat.tsx`)
   - צ'אט בזמן אמת עם צוות התמיכה
   - יתמוך בשיתוף קבצים והיסטוריית שיחות

2. **התראות בזמן אמת** (`src/components/RealTimeNotifications.tsx`)
   - התראות בזמן אמת על שינויים
   - יתמוך בהתראות דחיפה, אימייל ו-SMS

3. **שיחות וידאו** (`src/components/VideoChat.tsx`)
   - שיחות וידאו עם צוות הפרויקט
   - יתמוך בשיתוף מסך והקלטת שיחות

### חתימות דיגיטליות
1. **חתימה דיגיטלית** (`src/components/DigitalSignature.tsx`)
   - חתימה דיגיטלית על מסמכים
   - יתמוך בחתימה גרפית ואימות מאובטח

2. **אימות חתימה** (`src/components/SignatureVerification.tsx`)
   - אימות חתימות דיגיטליות
   - יתמוך באימות ביומטרי, SMS ואימייל

3. **ארכיון חתימות** (`src/components/SignatureArchive.tsx`)
   - ארכיון של חתימות דיגיטליות
   - יתמוך בחיפוש וייצוא חתימות

### תשלומים
1. **תשלומים מקוונים** (`src/components/OnlinePayments.tsx`)
   - מערכת תשלומים מקוונת
   - יתמוך בכרטיסי אשראי, PayPal ו-Apple Pay

2. **מעקב תשלומים** (`src/components/PaymentTracking.tsx`)
   - מעקב אחר היסטוריית תשלומים
   - יתמוך בתזכורות ודוחות

3. **חשבוניות דיגיטליות** (`src/components/DigitalInvoices.tsx`)
   - הפקת חשבוניות דיגיטליות
   - יתמוך בחשבוניות מס וקבלות

### אינטגרציות חיצוניות
1. **אינטגרציה עם מערכות עירוניות** (`src/services/MunicipalIntegration.ts`)
   - חיבור למערכות GIS עירוניות
   - יתמוך במערכות תכנון ורישוי

2. **אינטגרציה עם טאבו** (`src/services/TabuIntegration.ts`)
   - קבלת נתונים ישירות מטאבו
   - יתמוך בנסחי טאבו ובעלויות

3. **אינטגרציה עם רמ"י** (`src/services/RamiIntegration.ts`)
   - קבלת נתונים מרשות מקרקעי ישראל
   - יתמוך במידע על קרקעות וחוזי חכירה

### בינה מלאכותית
1. **חיזוי התקדמות פרויקטים** (`src/services/ProjectPrediction.ts`)
   - חיזוי זמני סיום פרויקטים
   - יתמוך בחיזוי עיכובים ועלויות

2. **זיהוי בעיות פוטנציאליות** (`src/services/ProblemDetection.ts`)
   - זיהוי מוקדם של בעיות אפשריות
   - יתמוך בזיהוי סיכונים ועיכובים

3. **המלצות אישיות** (`src/services/PersonalRecommendations.ts`)
   - המלצות מותאמות אישית לדיירים
   - יתמוך בהמלצות תוכן ופעולות

### אפליקציית מובייל
1. **אפליקציית iOS ואנדרואיד** (`mobile/App.tsx`)
   - אפליקציה ייעודית למכשירים ניידים
   - יתמוך ב-iOS, אנדרואיד ו-PWA

2. **התראות פוש** (`mobile/services/PushNotifications.ts`)
   - התראות פוש למכשירים ניידים
   - יתמוך בהתראות מותאמות אישית

3. **סריקת מסמכים** (`mobile/components/DocumentScanner.tsx`)
   - סריקת מסמכים ממכשיר נייד
   - יתמוך ב-OCR וזיהוי אוטומטי

### דוחות מתקדמים
1. **דוחות מותאמים אישית** (`src/components/CustomReports.tsx`)
   - יצירת דוחות לפי דרישה
   - יתמוך בתבניות דוחות ושיתוף

2. **ייצוא בפורמטים שונים** (`src/services/ExportService.ts`)
   - ייצוא נתונים בפורמטים שונים
   - יתמוך ב-PDF, Excel, CSV, JSON ו-XML

3. **דוחות אוטומטיים** (`src/services/AutomaticReports.ts`)
   - שליחת דוחות תקופתיים אוטומטית
   - יתמוך בדוחות יומיים, שבועיים וחודשיים

### ניהול מסמכים מתקדם
1. **עריכת מסמכים מקוונת** (`src/components/OnlineDocumentEditor.tsx`)
   - עריכת מסמכים ישירות באתר
   - יתמוך בעריכת טקסט, PDF ותמונות

2. **גרסאות מסמכים** (`src/components/DocumentVersions.tsx`)
   - שמירת גרסאות קודמות של מסמכים
   - יתמוך בהשוואת גרסאות ושחזור

3. **שיתוף מסמכים** (`src/components/DocumentSharing.tsx`)
   - שיתוף מסמכים עם גורמים חיצוניים
   - יתמוך בהרשאות צפייה/עריכה וסיסמאות

### ניהול פרויקטים מתקדם
1. **לוח גאנט** (`src/components/GanttChart.tsx`)
   - תצוגת לוח גאנט לניהול זמנים
   - יתמוך בתלויות ואבני דרך

2. **ניהול משימות** (`src/components/TaskManagement.tsx`)
   - הקצאת משימות לגורמים שונים
   - יתמוך ביצירת משימות ומעקב

3. **מעקב אחר ביצועים** (`src/components/PerformanceTracking.tsx`)
   - מדידת ביצועים והתקדמות
   - יתמוך בדוחות התקדמות והשוואה לתכנון

## סיכום 📊
- **סה"כ מודולים**: 67
- **מודולים שפותחו**: 31
- **מודולים שטרם פותחו**: 36